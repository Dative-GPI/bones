name: Build & Tests

on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Build and Run dotnet tests
        run: docker-compose up --build tests
      
      - name: Build and Run tests ui
        run: docker-compose up --build tests-ui
